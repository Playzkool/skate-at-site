import { defineConfig } from 'astro/config'

// Served as a GitHub Pages "project page" at https://playzkool.github.io/skate-at-site/
export default defineConfig({
  site: 'https://playzkool.github.io',
  base: '/skate-at-site',
})
